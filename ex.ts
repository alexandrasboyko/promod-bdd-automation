function a( data: { [ key: string] : string} ) {

}
const obj = {};
obj['k1'] = '77'

init(selector, childName, Child, CoolectionChild){
	const = typeof selector === 'string' ? this.toot.$(selector) : selector;
}